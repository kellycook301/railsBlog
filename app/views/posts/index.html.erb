<h1 class="title">Blog Posts</h1>
<% @posts.each do |post| %>
<div class="card">
    <h4><%= post.title %></h4>
    <p><%= post.body %></p>
    <%= link_to "Read More", post_path(post), :class => 'btn btn default' %>
    </div>
<% end %>